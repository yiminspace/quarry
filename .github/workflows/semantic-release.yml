name: Semantic Release

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      force:
        description: "Force bump level for one-off backfill release"
        required: false
        type: choice
        default: none
        options:
          - none
          - patch
          - minor
          - major

permissions:
  contents: read

jobs:
  release:
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref_name }}
      cancel-in-progress: false
    permissions:
      actions: write
      contents: write
    steps:
      - name: Checkout release branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.ref_name }}

      - name: Force release branch to workflow SHA
        run: git reset --hard ${{ github.sha }}

      - name: Semantic release (automatic bump)
        id: automatic
        if: ${{ !(github.event_name == 'workflow_dispatch' && github.event.inputs.force != 'none') }}
        uses: python-semantic-release/python-semantic-release@v10.5.3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          git_committer_name: github-actions
          git_committer_email: actions@users.noreply.github.com

      - name: Semantic release (forced bump)
        id: forced
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.force != 'none' }}
        uses: python-semantic-release/python-semantic-release@v10.5.3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          force: ${{ github.event.inputs.force }}
          git_committer_name: github-actions
          git_committer_email: actions@users.noreply.github.com

      - name: Dispatch PyPI publication
        if: ${{ steps.automatic.outputs.released == 'true' || steps.forced.outputs.released == 'true' }}
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ steps.automatic.outputs.tag || steps.forced.outputs.tag }}
        run: gh workflow run release.yml --ref "$RELEASE_TAG" -f tag="$RELEASE_TAG"
